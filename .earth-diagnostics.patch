--- a/tools/earth/browser-test.py
+++ b/tools/earth/browser-test.py
@@ -18,10 +18,20 @@
    if os.getenv('CHROMIUM_PATH'):options['executable_path']=os.environ['CHROMIUM_PATH']
    browser=await p.chromium.launch(**options)
    page=await browser.new_page(viewport={'width':1440,'height':900})
-   errors=[];page.on('pageerror',lambda e:errors.append(str(e)))
-   page.on('console',lambda m:print(m.type,m.text,flush=True) if m.type=='error' else None)
+   errors=[];messages=[];page.on('pageerror',lambda e:errors.append(str(e)))
+   def log(m):
+    messages.append({'type':m.type,'text':m.text});print(m.type,m.text,flush=True)
+   page.on('console',log)
    await page.goto('http://localhost:8099/tests/earth/gpu.html');await page.wait_for_function('window.testReady')
-   gpu=await page.evaluate('window.runEarthTests()');await page.screenshot(path=str(output/'gpu-regression.png'))
+   try:
+    gpu=await page.evaluate('window.runEarthTests()')
+   except Exception as e:
+    state=await page.evaluate('({stage:window.testStage,progress:window.testProgress,errors:window.testErrors,deviceLost:window.testLost})')
+    failure={'error':str(e),'state':state,'console':messages,'pageErrors':errors,'browser':browser.version,'launchArgs':options['args']}
+    (output/'gpu-failure.json').write_text(json.dumps(failure,indent=2));print(json.dumps(failure,indent=2),flush=True)
+    await page.screenshot(path=str(output/'gpu-failure.png'))
+    raise
+   await page.screenshot(path=str(output/'gpu-regression.png'))
    # Keep live provider availability out of deterministic tests.
    await page.route('https://raw.githubusercontent.com/**',lambda r:r.fulfill(content_type='application/json',body=json.dumps({'features':[{'geometry':{'type':'Polygon','coordinates':[[[110,-45],[155,-45],[155,-10],[110,-10],[110,-45]]]}}]})))
    await page.route('https://s3.amazonaws.com/**',lambda r:r.fulfill(content_type='image/png',body=png()))
--- a/tests/earth/gpu.html
+++ b/tests/earth/gpu.html
@@ -3,15 +3,18 @@
 import {createTerrain} from '../../src/earth/terrain.js';
 window.testReady=true;
 window.runEarthTests=async()=>{
- const errors=[];const renderer=await new EarthRenderer(document.getElementById('canvas'),{onError:e=>errors.push(String(e)),onProgress:e=>console.log(e.entry,e.phase)}).init({width:256,height:160});window.renderer=renderer;
+ const stage=name=>{window.testStage=name;console.log('STAGE',name);};
+ const errors=window.testErrors=[],progress=window.testProgress=[];stage('init');
+ const renderer=new EarthRenderer(document.getElementById('canvas'),{onError:e=>{errors.push(String(e));console.error(String(e));},onProgress:e=>{progress.push(e);console.log('PROGRAM',JSON.stringify(e));}});window.renderer=renderer;
+ await renderer.init({width:256,height:160});renderer.device.lost.then(e=>{window.testLost={reason:e.reason,message:e.message};console.error('DEVICE LOST',JSON.stringify(window.testLost));});stage('land');
  await renderer.setLand({features:[{geometry:{type:'Polygon',coordinates:[[[-20,-20],[20,-20],[20,20],[-20,20],[-20,-20]]]}}]});
- const terrain=createTerrain(0,0,1000,()=>0),osm={elements:[{type:'way',id:42,tags:{building:'yes',height:'30',name:'Known-height fixture'},geometry:[{lat:-.0003,lon:-.0003},{lat:-.0003,lon:.0003},{lat:.0003,lon:.0003},{lat:.0003,lon:-.0003},{lat:-.0003,lon:-.0003}]}]};
- await renderer.setScene(terrain,osm);const before=await renderer.runtime.read(renderer.resources.Genomes);
- const camera={lat:-.001,lon:0,height:22,yaw:0,pitch:-3,grid:false,provenance:false};await renderer.frame(camera);
- const pick=await renderer.pick(128,80);const image=await renderer.runtime.read(renderer.resources.Pixels,Uint32Array);let colored=0,picked=0;for(let i=0;i<256*160;i++){if((image[i]&0xffffff)!==0)colored++;if(image[256*160+i]===1)picked++;}
- await renderer.setScene(terrain,osm);const after=await renderer.runtime.read(renderer.resources.Genomes);if(before[0]!==30||after.some((v,i)=>v!==before[i]))throw Error('Farm changed known height or was non-deterministic');
+ stage('prepare');const terrain=createTerrain(0,0,1000,()=>0),osm={elements:[{type:'way',id:42,tags:{building:'yes',height:'30',name:'Known-height fixture'},geometry:[{lat:-.0003,lon:-.0003},{lat:-.0003,lon:.0003},{lat:.0003,lon:.0003},{lat:.0003,lon:-.0003},{lat:-.0003,lon:-.0003}]}]};
+ stage('setScene');await renderer.setScene(terrain,osm);stage('readGenomes');const before=await renderer.runtime.read(renderer.resources.Genomes);
+ stage('surface');const camera={lat:-.001,lon:0,height:22,yaw:0,pitch:-3,grid:false,provenance:false};await renderer.frame(camera);
+ stage('pick');const pick=await renderer.pick(128,80);const image=await renderer.runtime.read(renderer.resources.Pixels,Uint32Array);let colored=0,picked=0;for(let i=0;i<256*160;i++){if((image[i]&0xffffff)!==0)colored++;if(image[256*160+i]===1)picked++;}
+ stage('repeat farm');await renderer.setScene(terrain,osm);const after=await renderer.runtime.read(renderer.resources.Genomes);if(before[0]!==30||after.some((v,i)=>v!==before[i]))throw Error('Farm changed known height or was non-deterministic');
  if(!pick||pick.id!=='way/42'||picked<100)throw Error('Source footprint intersection / picking failed: '+JSON.stringify({pick,picked}));
- await renderer.frame({...camera,lat:10,height:14000000,pitch:-90});const globe=await renderer.runtime.read(renderer.resources.Pixels,Uint32Array);const unique=new Set(globe.slice(0,256*160)).size;if(unique<100)throw Error('Globe did not render');
- if(errors.length)throw Error(errors.join('\n'));return {programs:4,knownHeight:before[0],exactFarm:true,pickedPixels:picked,coloredPixels:colored,globeColors:unique,adapter:renderer.runtime.describe(),errors};
+ stage('globe');await renderer.frame({...camera,lat:10,height:14000000,pitch:-90});const globe=await renderer.runtime.read(renderer.resources.Pixels,Uint32Array);const unique=new Set(globe.slice(0,256*160)).size;if(unique<100)throw Error('Globe did not render');
+ stage('complete');if(errors.length)throw Error(errors.join('\n'));return {programs:4,knownHeight:before[0],exactFarm:true,pickedPixels:picked,coloredPixels:colored,globeColors:unique,adapter:renderer.runtime.describe(),errors};
 };
 </script>
